import fs from 'fs/promises';import os from 'os';import path from 'path';
process.env.DATA_DIR=await fs.mkdtemp(path.join(os.tmpdir(),'iva-projects-'));const {listProjects,getProject,updateProject}=await import('../projects/store.js');let failures=0;function check(name,value){const ok=Boolean(value);console.log(`${ok?'PASS':'FAIL'} ${name}`);if(!ok)failures++}
const projects=await listProjects(),heat=projects.find(item=>item.id==='heat-hero');check('Heat Hero vorhanden',heat);check('Projektphasen sichtbar',heat.phases.length===5);check('Automationen sichtbar',heat.automations.length>=6);check('Planbar aktiv',heat.automations.some(item=>item.id==='planbar-weekly'&&item.status==='active'));check('Herstellerlauf pausiert',heat.automations.some(item=>item.id==='manufacturer-daily'&&item.status==='paused'));await updateProject('heat-hero',{status:'active'});check('Projektupdate bleibt gespeichert',(await getProject('heat-hero')).status==='active');const html=await fs.readFile(new URL('../public/projects.html',import.meta.url),'utf8'),js=await fs.readFile(new URL('../public/projects.js',import.meta.url),'utf8');check('Projektseite und Automationsansicht vorhanden',html.includes('Phasen · Bereiche · Automationen')&&js.includes('Automationen und Automatismen'));console.log(failures?`${failures} Fehler`:'Projektbereich erfolgreich verifiziert.');process.exit(failures?1:0);
